function demo(a,...args){//剩余运算符
	console.log(a,args)
}
	demo(...[1,2,3],...[4,5,6])//参数会被序列化
	
	// demo(...{a:1,b:2}) //报错


<1> 当...被函数执行扩号包裹的时候,...里面的参数必须具备遍历器接口: 字符串 数组 Set Map
		 参数会被序列化:x,x,x,
		 所以a能接收到第一个参数
		 
<2> 而将...放在函数定义的形参最后一位则是剩余运算符 会把 序列化的参数变成数组