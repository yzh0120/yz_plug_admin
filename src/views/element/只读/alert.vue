<template>
</template>

<script>
</script>

<style>
</style>
<!-- 
this.$message.error("") 
this.$message({
          message: '恭喜你，这是一条成功消息',
          type: 'success'
        });
		
 -->
 
 
<!-- 	
let con = await this.$confirm(`确定移除 ？`).catch((e) => {});
let con = await this.$confirm("确认该项目申请当期上会吗？", "提示", {
        confirmButtonText: "确定",
        cancelButtonText: "取消",
        type: "warning",
      }).catch((e) => {});

if (con == "confirm") {
	//保存修改
} else{
	//右上角取消 或者 取消按钮
}




持续显示
const copymsg = this.$message({
            message: "正在复制项目。。。",
            type: "warning",
            duration: 0,
          });

 copymsg.close();
-->

<!-- 
 $msgbox(options)                                                                   自定义
 $alert(message, title, options) 或 $alert(message, options)                          没有取消按钮
 $confirm(message, title, options) 或 $confirm(message, options)                      有取消按钮
 $prompt(message, title, options) 或 $prompt(message, options)                        提交内容
 -->
 
